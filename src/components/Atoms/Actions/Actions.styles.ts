import { css } from '@emotion/react';

const actions = css`
	width: 70px;
	display: flex;
	justify-content: space-around;

	img {
		cursor: pointer;
	}
`;
const styles = {
	actions,
};

export default styles;